let numero=parseInt(prompt("->Ingrese el número a operar: "));
const cuadrado=Math.pow(numero, 2);
const cubo=Math.pow(numero, 3);
const raiz=Math.sqrt(numero);
alert(`->Cuadrado de ${numero} = ${cuadrado}\n->Cubo de ${numero} = ${cubo}\n->Raiz cuadrada de ${numero} = ${raiz}` );